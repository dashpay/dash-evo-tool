use crate::model::wallet::kms::Kms;

/// Simple Key Management Service (KMS) implementation for managing wallet keys.
pub struct SimpleKms {}

impl SimpleKms {
    /// Creates a new instance of `SimpleKms`.
    pub fn new() -> Self {
        SimpleKms {}
    }
}
